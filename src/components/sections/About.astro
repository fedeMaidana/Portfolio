---
import Card from '@/components/ui/Card.astro';
import { getLangFromUrl, useTranslations } from '@/i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Card class="about" ariaLabelledby="about-title">
    <h2 id="about-title" class="about-title">{t('about.title')}</h2>

    <div class="bio-container">
        <div class="trajectory">
            <span class="path-node past">Frontend</span>
            <span class="path-arrow" aria-hidden="true">→</span>
            <span class="path-node present">Backend</span>
        </div>

        <div class="bio-blocks">
            <p class="history">{t('about.history')}</p>

            <div class="focus-block">
                <p class="current-focus">
                    {t('about.focus.1')}<strong>Rust</strong>{t('about.focus.2')}
                </p>
            </div>

            <p class="perspective">
                {t('about.perspective.1')}<strong>{t('about.perspective.strong1')}</strong>{
                    t('about.perspective.2')
                }<strong>{t('about.perspective.strong2')}</strong>{t('about.perspective.3')}
            </p>
        </div>
    </div>
</Card>

<style>
    @scope (.about) {
        .bio-container {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        /* ─── Trajectory ─── */

        .trajectory {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .path-node {
            font-family: var(--font-mono);
            font-size: var(--text-sm);
            font-weight: 700;
            padding: 0.3rem 0.75rem;
            border-radius: 100px;
            border: 1px solid var(--card-border);
            transition:
                border-color 0.3s ease,
                color 0.3s ease;
        }

        .path-node.past {
            color: var(--text-muted);
        }

        .path-node.present {
            color: var(--accent);
            border-color: var(--accent-hover-border);
            background-color: var(--accent-hover-bg);
        }

        .path-arrow {
            font-family: var(--font-mono);
            font-size: var(--text-sm);
            color: var(--text-muted);
            opacity: 0.4;
        }

        /* ─── Bio blocks ─── */

        .bio-blocks {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .history,
        .current-focus,
        .perspective {
            font-size: var(--text-sm);
            line-height: 1.8;
            color: color-mix(in srgb, var(--text), transparent 22%);

            & strong {
                color: color-mix(in srgb, var(--accent), transparent 10%);
                font-weight: 600;
            }
        }

        /* ─── Focus highlight ─── */

        .focus-block {
            padding: 1rem 1.15rem;
            border-radius: var(--radius-md);
            background-color: color-mix(in srgb, var(--accent-hover-bg), transparent 40%);
            transition:
                background-color 0.3s ease,
                border-color 0.3s ease;
        }

        .focus-block .current-focus {
            margin: 0;
        }
    }

    @media (max-width: 600px) {
        @scope (.about) {
            .bio-container {
                gap: 1.25rem;
            }

            .bio-blocks {
                gap: 0.85rem;
            }

            .history,
            .current-focus,
            .perspective {
                font-size: 0.8rem;
                line-height: 1.6;
            }

            .focus-block {
                padding: 0.85rem 0.9rem;
            }
        }
    }
</style>
